Transformer
    Transformer, Attention (2017.6 LLM)
-- < question > ---------------------------------------------------
為什麼論文中的 attention 是 pixel 級別的
    attention weights 是 每次 forward 時動態算出來的 softmax 分數


-- <tech term> ---------------------------------------------------

Self Attention 與 Cross Attention
    前提
        Attention 一定是找 "多個向量資訊" 之間的關係
    Self Attention = Multi-Head Self-Attention block
        每個 向量 都要跟"同一組" 向量資訊 的其他 向量 計算 attention
        Q, K, V 都來自同一個輸入 X
    Cross Attention = Multi-Head Attention block
        每個 向量 都要跟"另一組" 向量資訊 的其他 向量 計算 attention
        Q 來自序列 A, K, V 來自序列 B
    相似點
        計算方法是完全相同的

    Multi-Head 意義

Positional encoding (PE)
    fix (in paper attention is what you need)
        加入位置資訊
            真的要相加，在這裡 concat 不合理
        PE(pos, 2i)   = sin(pos / 10000^(2i/d_model))
        PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))
            pos : 真正要 encode 的數字位置
            d_model : 總共要 encode 的維度 (因為要變成 vector)
            i : vector 中第幾個數字
            10000 是實驗得出來的數字
        ?? 為什麼要用 sin cos ?? 3/11 8:00 ??
    Rope 
        in Lama3 Qwen3
        根據 pos 距離 旋轉的角度會變大
    


FFN = FCL 1(Up-projection) + activation function + FCL 2(Down-projection) ...
    用於 特徵分析與總結
    運算
        對單一位置的特徵做深層轉換，並不會做跨位置的總結
            跨位置指的是 不同query的輸出
            也就是 不同query 之間 的 資訊"不會"互相流通

Attention : 主要在看的區域
    計算的方法
        average attention distances
            An image is worth 16x16 words: Transformers for image recognition at scale. In International Conference on Learning Representations. (2021)
        average attention maps
        attention similarities between heads
            Deepvit: Towards deeper vision transformer. arXiv preprint arXiv:2103.11886.
        normalized loss landscape 
            Visualizing the loss landscape of neural nets. (2017)
